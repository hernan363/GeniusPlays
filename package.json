{
    "scripts": {
      "build": "vue-cli-service build",
      "deploy": "npm run build && gh-pages -d dist"
    },
    "dependencies": {
      "vue": "^3.0.0"
    },
    "devDependencies": {
      "@vue/cli-service": "^5.0.0",
      "gh-pages": "^3.2.3"
    }
  }